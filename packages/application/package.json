{
  "name": "@originos/application",
  "version": "0.5.0-alpha.1",
  "type": "module",
  "exports": "./src/index.ts",
  "dependencies": {
    "@originos/canonical-types": "workspace:*",
    "@originos/kernel": "workspace:*",
    "@originos/repository": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  }
}
